# Repository guidance

- Run `npm test` after changing TypeScript files.
- Keep Codex and Claude discovery rules inside their own adapters.
- Describe static evidence as discoverable context, not proof of model behavior.
- Do not include instruction contents in reports unless the user put that text in a context contract.
