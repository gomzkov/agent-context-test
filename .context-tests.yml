# yaml-language-server: $schema=./schema/context-tests.v1.schema.json
version: 1
task: maintain-agent-context-test
targets: [codex, claude]

assertions:
  - id: run-tests
    expect: available
    contains: "Run `npm test` after changing TypeScript files."

  - id: honest-language
    expect: available
    contains: "Describe static evidence as discoverable context, not proof of model behavior."
